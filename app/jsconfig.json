{
  "compilerOptions": {
    "baseUrl": ".",  // Isso define a base do caminho relativo
    "paths": {
      "@/*": ["src/*"]  // Mapeia o alias `@/` para a pasta `src`
    }
  },
  "include": ["src"]
}
